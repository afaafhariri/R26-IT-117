"""Deprecated placeholder. The FastAPI entry point is ``Architecture/main.py``.

Kept as an empty module to preserve the path that earlier scaffolding committed.
Do not import from here. Run the API via:

    cd Architecture && uvicorn main:app --port 8001
"""
